/**
 * The one screen the rating prompt has. Everything that decides when it appears
 * is in `lib/rating`, which the data layer can reach and a feature cannot.
 */
export { RatePromptSheet } from './RatePromptSheet'
